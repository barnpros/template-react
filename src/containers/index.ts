/**
 * Containers are implementations of components.
 * Where components are agnostic, or display only, containers use those components in specific use-cases.
 * Examples: Login Form, Profile Preview Card
 */

// Replace this empty object with an export of each component in your containers folder
// Example: export * from "./LoginForm"
export const container = {};
