/**
 * All type definitions should be stored in this folder.
 * This provides a known central location for types, which contributes to keeping the rest of the application clean and organized.
 * Files in this folder should follow this naming convention: `[scope].types.ts`.
 * Examples: form.types.ts, common.types.ts
 */

// Replace this empty object with an export of each type definition in your types folder
// Example: export * from "./form.types"
export const type = {};
